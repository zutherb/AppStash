akka {
  loglevel = DEBUG
  extensions = ["kamon.metric.Metrics"]
}

spray.can.server {
  request-timeout = 5s
}

http {
  host = "0.0.0.0"
  port = 18080
}

mongodb {
  host = "mongodb-node:27017"
  servers = [host]
  db = "shop"
  collection = "product"
}
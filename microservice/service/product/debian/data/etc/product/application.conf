akka {
  loglevel = DEBUG
}

# logger.reactivemongo=DEBUG

spray.can.server {
  request-timeout = 5s
}

http {
  host = "localhost"
  port = 18080
}

mongodb {
  host = "mongodb-node:27017"
  servers = [host]
  db = "shop"
  collection = "product"
}